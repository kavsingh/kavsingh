{/* eslint-disable-next-line tailwindcss/no-custom-classname */}
<div class="content">
	<slot />
</div>

<style>
	.content :global(ul),
	.content :global(p) {
		margin-block: 0 theme(spacing[2.5]);
	}

	.content :global(ul) {
		padding: 0;
	}

	.content :global(li) {
		display: flex;
		margin-block: 0 theme(spacing[1.5]);
	}

	.content :global(li::before) {
		display: block;
		margin-inline-end: theme(spacing.3);
		color: theme(colors.neutral.500);
		content: "•";
		font-weight: theme(fontWeight.semibold);
	}

	.content :global(a) {
		color: currentColor;
		text-decoration: underline !important;
	}

	@media screen and (prefers-color-scheme: dark) {
		li::before {
			color: theme(colors.neutral.400);
		}
	}

	@media print {
		.content :global(a) {
			text-decoration: none;
		}

		.content :global(a::after) {
			margin-inline-start: theme(spacing[1.5]);
			content: "[ " attr(href) " ]";
		}
	}
</style>
