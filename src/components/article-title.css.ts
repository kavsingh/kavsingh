import { style } from '@vanilla-extract/css'

export const titleStyle = style({
  fontWeight: 600,
})

export const seperatorStyle = style({
  margin: '0 0.4em',
  fontWeight: '400',
})

export const subtitleStyle = style({
  fontWeight: 400,
})
